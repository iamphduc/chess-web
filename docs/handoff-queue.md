# Handoff Queue

Inter-agent communication. **The human is the ultimate arbiter** — `BLOCKED` entries halt the orchestrator until acknowledged. **One line per entry** so the queue is graspable at a glance; never break an entry across multiple lines.

Format: `` - `[YYYY-MM-DD · TYPE · from → to · sprint: <slug> · slice: <code>]` <body> **Resolution:** pending `` (or `**Resolution:** <YYYY-MM-DD> — <what changed> [optional link to docs/decisions.md#anchor]`).

Entries are **date-keyed and append-only** (newest at the tail) — reference one by its `[date · from → to]` header plus a few words of its body, never by position (a same-day header can repeat; the body disambiguates). Glance at the tail to judge whether prune is due (>100 entries). `from` / `to` may be any of `engineer-junior` / `engineer-senior` / `orchestrator` / `sprint-planner` / `planner` / `human` — anyone can address anyone. Omit `slice:` for sprint-wide entries; omit `sprint:` for project-wide entries.

Types: `BLOCKED` halts · `PENDING` defers (defaults taken, knowingly-incomplete spots, scope-creep opportunities) · `SOLVED` informational, only emitted alongside a `BLOCKED` or `PENDING` to mark a related thing resolved inline.

Resolve inline (do not delete prematurely); if decision-worthy, write a one-liner to `docs/decisions.md` and link from the Resolution line. At sprint end the orchestrator drops the oldest **resolved** entries beyond 100 (by date) — no renumbering; unresolved entries (`Resolution: pending`) are never pruned.

---

- `[2026-06-04 · PENDING · sprint-planner → human · sprint: engine-foundation]` Sprint draft written to `docs/sprints/engine-foundation.md` (3 slices, 2 waves: W1 harness+gamestate, W2 tracer) — sprint-draft gate; review then re-invoke `/autopilot` to execute. **Resolution:** 2026-06-04 — human reviewed and re-invoked `/autopilot`; sprint execution authorized.
- `[2026-06-04 · PENDING · sprint-planner → human]` `docs/codebase-structure.md` `## Smoke recipe` is still an unfilled stub; harmless for engine-foundation (no runnable app surface; verified by tsc+build+Jest) but `/code`'s fail-closed runtime-smoke gate will halt a later sprint until it's filled. **Resolution:** pending
- `[2026-06-04 · BLOCKED · orchestrator → human]` Preflight blocked: workflow docs committed locally (`1dcd05a`) but pushing to `origin/main` was denied by the permission classifier; engineers branch off `origin/main` and need the protocol/templates there. Push `1dcd05a` (e.g. `! git push origin main`) or authorize main pushes, then re-invoke `/autopilot`. **Resolution:** 2026-06-04 — human chose PR-based merge; infra landed on `main` via PR (branch `chore/workflow-infra`) instead of a direct push.
- `[2026-06-04 · PENDING · engineer-junior → orchestrator · sprint: engine-foundation · slice: harness]` First harness attempt (CRA/Jest, PR #2) thrashed on a Windows test-discovery failure when run from a worktree under the `.claude/` dot-directory; needed a `testMatch` override. **Resolution:** 2026-06-04 — PR #2 closed; switched the engine test runner to **Vitest** at human's direction. See `docs/decisions.md` → "Vitest for the engine test suite, over CRA's Jest". Sprint re-sliced to 3 waves (harness → gamestate → tracer).
- `[2026-06-04 · PENDING · engineer-junior → orchestrator · sprint: engine-foundation · slice: harness]` Vitest 2.1.9 install needed `--legacy-peer-deps`: project pins `@types/node@^16` (CRA-era) which conflicts with vitest 2.x's `@types/node@^18 || >=20` peer. No runtime impact (actual Node is v24); tests + build green. A future slice could bump `@types/node` in deps to clear it cleanly. **Resolution:** pending
- `[2026-06-04 · PENDING · engineer-senior → orchestrator · sprint: engine-foundation · slice: gamestate]` `GameState` field sub-shapes are defensible defaults: `turn: PieceColor` union (not old `isWhiteTurn` boolean), `castling: {white:{kingSide,queenSide},black:{...}}`, `enPassant: readonly [y,x]|null`, `promotionCount`: 8-slot readonly tuple ordered `[WQ,WR,WB,WN,BQ,BR,BB,BN]`. `tracer`/`special-moves`/`cutover-cleanup` may revisit color encoding or enPassant shape. **Resolution:** pending
- `[2026-06-04 · PENDING · engineer-senior → orchestrator · sprint: engine-foundation · slice: gamestate]` `vitest.config.ts` does not resolve the tsconfig `baseUrl: ./src` aliases, so `game/...` imports fail under Vitest; gamestate used relative imports inside its owned files to stay green. A config-owning slice should add `vite-tsconfig-paths` (or `resolve.alias`) so engine modules can use `game/...` imports under test. **Resolution:** pending
- `[2026-06-04 · PENDING · engineer-senior → orchestrator · sprint: engine-foundation · slice: gamestate]` Git worktrees don't share `node_modules`; each engineer worktree must run `npm install --legacy-peer-deps` before tests/build (plain `npm install` is blocked by the `@types/node` peer conflict). `package-lock.json` unchanged. Future worktree dispatches must include this install step. **Resolution:** pending
- `[2026-06-04 · PENDING · engineer-senior → orchestrator · sprint: engine-foundation · slice: tracer]` `legalMoves` intentionally generates only white-pawn pushes and `applyMove` carries `castling`/`enPassant`/`promotionCount` through unchanged with no capture/special bookkeeping — the tracer minimum. Full move generation is the `move-generation` sprint; special-move bookkeeping is `special-moves`. By plan, not a gap. **Resolution:** pending
- `[2026-06-04 · PENDING · reviewer → orchestrator · sprint: engine-foundation · slice: review]` Reviewer ran four lenses over the sprint diff: no bugs, no security issues, nothing to simplify under delete-first → `PR: clean`. **Resolution:** 2026-06-04 — clean review, nothing shipped; sprint archived.
- `[2026-06-04 · PENDING · reviewer → orchestrator · sprint: engine-foundation · slice: review]` Worktree teardown required `git worktree remove --force` because worktrees accumulate untracked `node_modules` (per-worktree install). Protocol forbids `--force` for engineers; orchestrator should expect to `--force`-remove worktrees, or have engineers delete `node_modules` before teardown. Consider gitignoring/relocating to avoid. **Resolution:** pending
- `[2026-06-04 · PENDING · reviewer → orchestrator · sprint: engine-foundation · slice: review]` Intermittent: the first `npm install --legacy-peer-deps` in a fresh worktree produced a corrupt partial tree on Windows (npm optional-dep extraction bug; `@rollup/rollup-win32-x64-msvc` invalid). Fix = delete `node_modules` and reinstall once. Future worktree dispatches should retry-on-corrupt. **Resolution:** pending
- `[2026-06-04 · PENDING · reviewer → orchestrator · sprint: engine-foundation · slice: review]` Test nit: `game-state.test.ts` asserts castling/promotionCount non-aliasing for `white` only; `castling.black` independence is covered transitively. Cheap to add when `move-generation` next touches this file. **Resolution:** pending
- `[2026-06-04 · PENDING · sprint-planner → human · sprint: move-generation]` Sprint draft written to `docs/sprints/move-generation.md` (6 slices, 3 waves: W1 geometry → W2 sliding/knight/king-step/pawn → W3 dispatch+bounds-regression) — sprint-draft gate. Review then re-invoke `/autopilot` to execute. **Resolution:** 2026-06-04 — human re-invoked `/autopilot`; draft acked, sprint execution authorized. Admin-merge authorized for this session; run clock reset to 19:01:08Z.
- `[2026-06-04 · PENDING · engineer-senior → orchestrator · sprint: move-generation · slice: geometry]` Generator signature pinned as `(state: GameState, from: Position) => Move[]` (pseudo-legal; no turn/kind guard, no check filtering) in the `classify.ts` doc comment only. Wave-2 slices must import `Move`/`Position` from `../engine` and honor it. **Resolution:** pending
- `[2026-06-04 · PENDING · engineer-senior → orchestrator · sprint: move-generation · slice: geometry]` `occupant` returns `null` for off-board coords (folds bounds in), so ray generators needing the empty-vs-off-board distinction must still gate on `inBounds` before continuing a ray — documented in the helper's doc comment. **Resolution:** pending
- `[2026-06-04 · PENDING · engineer-senior → orchestrator · sprint: move-generation · slice: dispatch]` Engineers observed an uncommitted edit to `docs/sprints/move-generation.md` in the parent-repo working tree during cleanup. **Resolution:** 2026-06-04 — expected: that's the orchestrator's live status-board/run-counter bookkeeping, committed to `main` at sprint archive (along with the plan-row flip).
- `[2026-06-04 · PENDING · reviewer → orchestrator · sprint: move-generation · slice: review]` Four lenses clean over the sprint diff (`7fd436f..a91c128`, 12 files): pseudo-legal generation correct, both axes route through `inBounds`, bounds regression genuinely pins off-board destinations on both axes, scope discipline holds, tests non-tautological, no security surface → `PR: clean`, nothing shipped. **Resolution:** 2026-06-04 — clean review; sprint archived.
- `[2026-06-04 · PENDING · reviewer → orchestrator · sprint: move-generation · slice: sliding]` `slidingMoves` re-checks `piece === null` after the caller's guard — dead defensive branch; too trivial to refactor under delete-first rails, fold into any future touch. **Resolution:** pending
- `[2026-06-04 · PENDING · reviewer → orchestrator · sprint: move-generation · slice: geometry]` `classify.ts` doc for `pieceKind` says "matched suffix-first" but the impl is priority-ordered `includes` (PAWN→KNIGHT→BISHOP→ROOK→QUEEN→KING); logic correct for all 64 ids, only the prose is imprecise. **Resolution:** pending
- `[2026-06-04 · PENDING · reviewer → orchestrator · sprint: move-generation · slice: pawn]` Guard inconsistency: `pawn.ts` rejects non-pawn kinds defensively while `knight.ts`/`king.ts` don't; both documented as defensive-only and dispatched by kind, so behavior is correct either way. **Resolution:** pending
- `[2026-06-04 · PENDING · sprint-planner → human · sprint: king-safety-endgame]` Sprint draft written to `docs/sprints/king-safety-endgame.md` (3 slices, 3 sequential waves: W1 attack-map → W2 legal-filter → W3 game-status; all engineer-senior). Crux flagged: `attack-map` uses attack semantics (pawn capture-diagonals only) and must NOT call the soon-to-be-check-filtered `legalMoves` (circular-dep avoidance). Sprint-draft gate — review then re-invoke `/autopilot` to execute. **Resolution:** 2026-06-04 — human re-invoked `/autopilot` (same session); draft acked, execution authorized. Admin-merge authorization and reset run clock (19:01:08Z) carry over.
- `[2026-06-04 · PENDING · reviewer → orchestrator · sprint: king-safety-endgame · slice: review]` Four lenses clean over `000b398..e80fefb` (6 files, +828/−4): attack semantics correct (pawn diagonals only, turn-independent, no `legalMoves` dep), self-check filter tests the captured `mover` in the post-`applyMove` position (not `applied.turn`) and is non-re-entrant, game-end delegates legality entirely to `legalMoves`, mate/stalemate/pin positions are genuine (hand-checked, non-tautological), scope intact → `PR: clean`, nothing shipped. **Resolution:** 2026-06-04 — clean review; sprint archived.
- `[2026-06-04 · PENDING · sprint-planner → human · sprint: special-moves]` Sprint draft written to `docs/sprints/special-moves.md` (3 slices, 2 waves: W1 `engine-core` [applyMove rewrite for castle/en-passant/promotion + `Move.promotion?` field + quarantined promotion-id allocator in `moves/promotion.ts`] → W2 `castling` (king.ts) ∥ `pawn-special` (pawn.ts), parallel/disjoint). Design calls to review: (1) `Move` gains optional `promotion?` — cross-sprint contract for external-anchors (perft) + cutover adapter; (2) en-passant generation in `pawn.ts`, application in `applyMove`; (3) castle through-check lives in `king.ts` (filter can't see the transit square); (4) 4-promotions/type cap persists (quarantine limit); (5) smoke-recipe stub still unfilled — will block from `cutover-cleanup`. Sprint-draft gate — review then re-invoke `/autopilot` to execute. **Resolution:** 2026-06-04 — human replied `continue` (same session); draft acked, execution authorized. Admin-merge authorization and reset run clock (19:01:08Z) carry over. Stray `</content></invoke>` artifact at the draft's tail cleaned up on start.
- `[2026-06-04 · PENDING · engineer-senior → orchestrator · sprint: special-moves · slice: engine-core]` Promotion-id table is rebuilt from the `PieceType` enum directly in `moves/promotion.ts` instead of importing `PromotionBoard` from `src/constants.ts`, because `constants.ts` transitively imports baseUrl-aliased UI modules (`game/piece-moves`, `game/piece-type`) that Vitest can't resolve — importing it broke every engine test. Table is row-for-row identical to `PromotionBoard`; keeps the pure engine free of legacy UI coupling. Divergence risk if `PromotionBoard` ever changes; a follow-up could relocate `PromotionBoard` into the engine layer or add the vitest alias (relates to the long-standing tsconfig-alias-under-Vitest PENDING). **Resolution:** 2026-06-04 — reviewer hand-verified `PROMOTED_IDS` is row-for-row identical to `constants.ts` `PromotionBoard` (correct now). Divergence risk for future maintainers persists; fold into `cutover-cleanup` when the engine meets the UI layer.
- `[2026-06-04 · PENDING · engineer-senior → orchestrator · sprint: special-moves · slice: engine-core]` 5th-of-a-type promotion clamps to the `*Promoted4` id (counter still increments) rather than throwing, so `applyMove` stays total — the quarantine's 4-per-type cap, documented in the allocator. **Resolution:** pending
- `[2026-06-04 · PENDING · reviewer → orchestrator · sprint: special-moves · slice: review]` Four lenses clean over `e1582c9..39c1778` (9 files, +1088/−58). Hand-verified the two high-risk areas: `PROMOTED_IDS` row-for-row identical to `constants.ts` `PromotionBoard`; `applyMove` rights-maintenance (king moves / rook-leaves-corner / rook-captured-on-corner) + purity (fresh grid, `castling`, `enPassant`, `promotionCount`; no input aliasing) correct; castle legality check-gates origin/transit/dest only (b-file empty-not-check-gated); en-passant fires only on the matching `enPassant` square; four-way promotion on push+capture. Tests genuine, scope intact → `PR: clean`. **Resolution:** 2026-06-04 — clean review; sprint archived.
- `[2026-06-04 · PENDING · sprint-planner → human · sprint: external-anchors]` Sprint draft written to `docs/sprints/external-anchors.md` (3 slices, 2 waves: W1 `perft-start` (depth 1=20/2=400/3=8902, owns perft harness+helper) ∥ `recorded-game` (Scholar's-mate miniature, asserts each ply legal + final `gameStatus==="checkmate"`) → W2 `perft-tactical` (Kiwipete depth 1=48/2=2039, depends on perft-start's helper)). Test-only/additive — NO new `src/` module (perft mirrors `game-status.ts`'s board scan, stays in `__tests__/`). Design call: position construction is the risk — mirror `apply-move-special.test.ts`'s `stateWith` helper for Kiwipete's `KQkq` rights; depth-1 self-checks transcription; escape hatch to a simpler published position via PENDING if Kiwipete proves error-prone; depth-3 Kiwipete optional for runtime. Sprint-draft gate — review then re-invoke `/autopilot`. **Resolution:** pending
