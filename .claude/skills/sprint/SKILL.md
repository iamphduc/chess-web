---
name: sprint
description: Use when the user types /sprint or asks to draft the next sprint from a main plan.
---

Dispatch the `sprint-planner` subagent via the Agent tool, passing the user's args (if any) as the prompt.
