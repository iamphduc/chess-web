---
name: review
description: Use when the user types /review or asks to run the reviewer agent on a sprint.
---

Dispatch the `reviewer` subagent via the Agent tool, passing the user's args (if any) as the prompt.
